--- a/src/main/print-canon.c
+++ b/src/main/print-canon.c
@@ -240,6 +240,8 @@
   int top;
   int left;
   int emptylines;
+  int skip_empty_blocks;      /* MX920 CD mode: replace blank multiraster blocks with ESC (e advances (like Canon's driver) */
+  int pending_empty_blocks;
   int ncolors; /* number of colors to print with */
   int physical_xdpi, nozzle_ydpi, stepper_ydpi;
   int nozzles;   /* count of inkjets for one pass */
@@ -387,6 +389,12 @@
     STP_PARAMETER_LEVEL_BASIC, 1, 1, STP_CHANNEL_NONE, 1, 0
   },
   {
+    "CDNoMask", N_("CD tray: no disc mask"), "Color=No,Category=Basic Printer Setup",
+    N_("Print the whole square CD page without masking the disc shape (PVC card trays)"),
+    STP_PARAMETER_TYPE_BOOLEAN, STP_PARAMETER_CLASS_FEATURE,
+    STP_PARAMETER_LEVEL_BASIC, 1, 1, STP_CHANNEL_NONE, 1, 0
+  },
+  {
     "Duplex", N_("Double-Sided Printing"), "Color=No,Category=Basic Printer Setup",
     N_("Duplex/Tumble Setting"),
     STP_PARAMETER_TYPE_STRING_LIST, STP_PARAMETER_CLASS_FEATURE,
@@ -2636,6 +2644,7 @@
       /* media size codes for CD (and other media depending on printer model */
 
       if (!strcmp(name,"CD5Inch"))     return 0x53; /* CD Tray G --- arbitrary choice here, modify in ESC (P command */
+      if (!strcmp(name,"TrayJ"))       return 0x53; /* full-face disc tray page: same CD workaround path (-> 0x5b for tray J models) */
       /* if (!strcmp(name,"CD5Inch"))  return 0x56; */ /* CD Tray G-late */
       /* if (!strcmp(name,"CD5Inch"))  return 0x57; */ /* CD Tray H */
 
@@ -2877,6 +2886,9 @@
           (description->bounds.str, "CD3Inch", _("CD - 3 inch"));
         stp_string_list_add_string
           (description->bounds.str, "CDCustom", _("CD - Custom"));
+        if (!strcmp(caps->name,"PIXMA MX920") || !strcmp(caps->name,"PIXMA iP7200"))
+          stp_string_list_add_string
+            (description->bounds.str, "TrayJ", _("Disc Tray J - full face"));
       }else{
 	  const stp_papersize_list_t *paper_sizes =
 	    stpi_get_standard_papersize_list();
@@ -2947,8 +2959,8 @@
 	   strcmp(name, "CDYAdjustment") == 0)
     {
       const char* input_slot = stp_get_string_parameter(v, "InputSlot");
-      description->bounds.dimension.lower = -15;
-      description->bounds.dimension.upper = 15;
+      description->bounds.dimension.lower = -120;
+      description->bounds.dimension.upper = 120;
       description->deflt.dimension = 0;
       if (!input_slot || !strcmp(input_slot,"CD"))
 	description->is_active = 1;
@@ -3212,6 +3224,12 @@
       else
 	description->is_active = 0;
     }
+  else if (strcmp(name, "CDNoMask") == 0)
+    {
+      const char* input_slot = stp_get_string_parameter(v, "InputSlot");
+      description->deflt.boolean = 0;
+      description->is_active = (!input_slot || !strcmp(input_slot,"CD")) ? 1 : 0;
+    }
   else if (strcmp(name, "Duplex") == 0)
   {
     int offer_duplex=0;
@@ -3321,7 +3339,7 @@
   if (media_size)
     pt = stp_describe_papersize(v, media_size);
 
-  if(input_slot && !strcmp(input_slot,"CD"))
+  if(input_slot && !strcmp(input_slot,"CD") && !(media_size && !strcmp(media_size,"TrayJ")))
     cd = 1;
 
   stp_default_media_size(v, &width, &length);
@@ -3348,6 +3366,11 @@
     right_margin = MAX(right_margin, caps->border_right);
     top_margin = MAX(top_margin, caps->border_top);
     bottom_margin = MAX(bottom_margin, caps->border_bottom);
+      /* TrayJ (MX920 disc tray, full face): honour the paper's own left margin. Raster column 0 is always placed at
+       the ESC (p origin (80 dots), so a larger left margin moves page content left on the tray: 12.84pt = 10pt +
+       the 1 mm correction measured on 2026-09-16. */
+    if (pt && media_size && !strcmp(media_size,"TrayJ") && !strcmp(caps->name,"PIXMA MX920"))
+      left_margin = pt->left;
   }
 
   /* temporarily limit to non-CD media until page size code moved here from do_print */
@@ -3846,8 +3869,13 @@
 
   const char* input_slot = stp_get_string_parameter(v, "InputSlot");
   int print_cd = (input_slot && (!strcmp(input_slot, "CD")));
+  int trayj_mx920 = 0; /* MX920 + PageSize=TrayJ: whole-tray-face page, Canon-matching geometry */
 
   stp_dprintf(STP_DBG_CANON, v,"setPageMargins2: print_cd = %d\n",print_cd);
+  /* MX920: CD pages report 0x53 (tray G placeholder) here; the ESC (P workaround later maps it to 0x5b (tray J).
+     Do the same for the paper-size table so fix_papersize() yields Canon's 3071x5311 tray-J page. */
+  if ( print_cd && !(strcmp(init->caps->name,"PIXMA MX920")) && arg_ESCP_1 == 0x53 )
+    arg_ESCP_1 = 0x5b;
 
   test_cd = 1;
   /*
@@ -4186,7 +4214,7 @@
   }
 
   /* Tray J */
-  if ( (print_cd) && !(strcmp(init->caps->name,"PIXMA iP7200")) && (test_cd==1) ) {
+  if ( (print_cd) && ( !(strcmp(init->caps->name,"PIXMA iP7200")) || !(strcmp(init->caps->name,"PIXMA MX920")) ) && (test_cd==1) && !(stp_get_string_parameter(v, "PageSize") && !strcmp(stp_get_string_parameter(v, "PageSize"), "TrayJ")) ) {
 
     init->page_width +=8;
     init->page_height +=44;
@@ -4243,7 +4271,7 @@
 
   if ((init->caps->features & CANON_CAP_px) ) {
     /* workaround for CD writing that uses CANON_CAP_px --- fix with capabilities */
-    if ( !( input_slot && !(strcmp(input_slot,"CD")) ) || !(strcmp(init->caps->name,"PIXMA iP4500")) || !(strcmp(init->caps->name,"PIXMA iP4600")) || !(strcmp(init->caps->name,"PIXMA iP4700")) || !(strcmp(init->caps->name,"PIXMA iP4800")) || !(strcmp(init->caps->name,"PIXMA iP4900")) || !(strcmp(init->caps->name,"PIXMA iP7200")) || !(strcmp(init->caps->name,"PIXMA MP980")) || !(strcmp(init->caps->name,"PIXMA MP990")) || !(strcmp(init->caps->name,"PIXMA MG5200")) || !(strcmp(init->caps->name,"PIXMA MG5300")) || !(strcmp(init->caps->name,"PIXMA MG6100")) || !(strcmp(init->caps->name,"PIXMA MG6200")) || !(strcmp(init->caps->name,"PIXMA MG6500")) || !(strcmp(init->caps->name,"PIXMA MG6700")) || !(strcmp(init->caps->name,"PIXMA MG7500")) || !(strcmp(init->caps->name,"PIXMA MG7700")) || !(strcmp(init->caps->name,"PIXMA MG8100")) || !(strcmp(init->caps->name,"PIXMA MG8200")) || !(strcmp(init->caps->name,"PIXMA iP9910")) || !(strcmp(init->caps->name,"PIXMA MP710")) || !(strcmp(init->caps->name,"PIXMA iP3000")) || !(strcmp(init->caps->name,"PIXMA Pro9000")) || !(strcmp(init->caps->name,"PIXMA iP8700")) || !(strcmp(init->caps->name,"PIXMA iX6800")) || !(strcmp(init->caps->name,"PIXMA TS8000")) )
+    if ( !( input_slot && !(strcmp(input_slot,"CD")) ) || !(strcmp(init->caps->name,"PIXMA iP4500")) || !(strcmp(init->caps->name,"PIXMA iP4600")) || !(strcmp(init->caps->name,"PIXMA iP4700")) || !(strcmp(init->caps->name,"PIXMA iP4800")) || !(strcmp(init->caps->name,"PIXMA iP4900")) || !(strcmp(init->caps->name,"PIXMA iP7200")) || !(strcmp(init->caps->name,"PIXMA MX920")) || !(strcmp(init->caps->name,"PIXMA MP980")) || !(strcmp(init->caps->name,"PIXMA MP990")) || !(strcmp(init->caps->name,"PIXMA MG5200")) || !(strcmp(init->caps->name,"PIXMA MG5300")) || !(strcmp(init->caps->name,"PIXMA MG6100")) || !(strcmp(init->caps->name,"PIXMA MG6200")) || !(strcmp(init->caps->name,"PIXMA MG6500")) || !(strcmp(init->caps->name,"PIXMA MG6700")) || !(strcmp(init->caps->name,"PIXMA MG7500")) || !(strcmp(init->caps->name,"PIXMA MG7700")) || !(strcmp(init->caps->name,"PIXMA MG8100")) || !(strcmp(init->caps->name,"PIXMA MG8200")) || !(strcmp(init->caps->name,"PIXMA iP9910")) || !(strcmp(init->caps->name,"PIXMA MP710")) || !(strcmp(init->caps->name,"PIXMA iP3000")) || !(strcmp(init->caps->name,"PIXMA Pro9000")) || !(strcmp(init->caps->name,"PIXMA iP8700")) || !(strcmp(init->caps->name,"PIXMA iX6800")) || !(strcmp(init->caps->name,"PIXMA TS8000")) )
       /* need to check if iP9910, MP710, iP3000, Pro9000 use Esc (p */
       {
 
@@ -4313,7 +4341,7 @@
 	  border_bottom2 = border_bottom + adjust_tray_H_bottom;
 	}
 
-	if ( (print_cd) && !(strcmp(init->caps->name,"PIXMA iP7200")) && (test_cd==1) ) {
+	if ( (print_cd) && ( !(strcmp(init->caps->name,"PIXMA iP7200")) || !(strcmp(init->caps->name,"PIXMA MX920")) ) && (test_cd==1) && !(stp_get_string_parameter(v, "PageSize") && !strcmp(stp_get_string_parameter(v, "PageSize"), "TrayJ")) ) {
 	  border_left2   = border_left + adjust_tray_J_left;
 	  border_right2  = border_right + adjust_tray_J_right;
 	  border_top2    = border_top + adjust_tray_J_top;
@@ -4330,6 +4358,17 @@
 	/* this does not seem to need adjustment, so use original borders */
 	area_right = border_left * unit / 72;
 	area_top = border_top * unit / 72;
+	/* TrayJ (whole tray face) on MX920: Canon's own tray-J origin is (80,70) @600dpi; +24 dots right = the measured 1 mm */
+	trayj_mx920 = ( !(strcmp(init->caps->name,"PIXMA MX920")) && stp_get_string_parameter(v, "PageSize") && !strcmp(stp_get_string_parameter(v, "PageSize"), "TrayJ") );
+	if ( trayj_mx920 ) {
+	  area_right = 80;       /* Canon's exact value: 56 (Canon-24) made the firmware abort the job after the tray scan */
+	  area_top = 70;         /* Canon's value; raising it moved the image TOWARD the leading edge, so it is not the down-shift lever */
+		  /* legacy medium fields: Canon's TrayJ job declares its 2911x5122 printable area here,
+	     (349.32+1)*5/6 = 292 and (614.64+1)*5/6 = 513. Declaring the 10.01in template page (580) instead
+	     tells the firmware the medium is 1.16in longer than the tray face. */
+	  printable_width  = 292;
+	  printable_length = 513;
+	}
 
 	if ( (init->caps->features & CANON_CAP_BORDERLESS) &&
 	     !(print_cd) && stp_get_boolean_parameter(v, "FullBleed") ) {
@@ -4375,7 +4414,11 @@
 	  stp_put32_be((init->page_height - border_top2 - border_bottom2 ) * unit / 72,v); /* area_length */
 	}
 	else {
-	  if ( (print_cd) && (test_cd==1) ) { /* bordered for CD */
+	  if ( trayj_mx920 ) { /* TrayJ: Canon's printable area 2911x5122 minus the 24-dot right shift */
+	    stp_put32_be(2911,v); /* area_width  (Canon's) */
+	    stp_put32_be(5122,v); /* area_length (Canon's) */
+	  }
+	  else if ( (print_cd) && (test_cd==1) ) { /* bordered for CD */
 	    stp_put32_be(init->page_width * unit / 72,v); /* area_width */
 	    stp_put32_be(init->page_height * unit / 72,v); /* area_length */
 	  }
@@ -5101,7 +5144,7 @@
       canon_cmd(v,ESC28,0x72, 1, 0x68);
     }
     /* CD mode only -- no ESC (r at all otherwise */
-    else if ( (init->mode->flags & MODE_FLAG_CD) && ( !(strcmp(init->caps->name,"PIXMA iP4900")) || !(strcmp(init->caps->name,"PIXMA MG5200")) || !(strcmp(init->caps->name,"PIXMA MG5300")) || !(strcmp(init->caps->name,"PIXMA MG6200")) || !(strcmp(init->caps->name,"PIXMA MG8200")) || !(strcmp(init->caps->name,"PIXMA TS8000")) ) ) {
+    else if ( (init->mode->flags & MODE_FLAG_CD) && ( !(strcmp(init->caps->name,"PIXMA iP4900")) || !(strcmp(init->caps->name,"PIXMA MG5200")) || !(strcmp(init->caps->name,"PIXMA MG5300")) || !(strcmp(init->caps->name,"PIXMA MG6200")) || !(strcmp(init->caps->name,"PIXMA MG8200")) || !(strcmp(init->caps->name,"PIXMA TS8000")) || !(strcmp(init->caps->name,"PIXMA MX920")) ) ) {
       canon_cmd(v,ESC28,0x72, 1, 0x68); /* same as above case? */
     }
     /* other cases here */
@@ -5731,7 +5774,7 @@
 static void setup_page(stp_vars_t* v,canon_privdata_t* privdata){
   const char *media_source = stp_get_string_parameter(v, "InputSlot");
   const char *cd_type = stp_get_string_parameter(v, "PageSize");
-  int print_cd = (media_source && (!strcmp(media_source, "CD")));
+  int print_cd = (media_source && (!strcmp(media_source, "CD")) && !(stp_get_string_parameter(v, "PageSize") && !strcmp(stp_get_string_parameter(v, "PageSize"), "TrayJ")));
   stp_dimension_t page_left,
                   page_top,
                   page_right,
@@ -5800,6 +5843,19 @@
   } else {
     privdata->left -= page_left;
     privdata->top -= page_top; /* checked in Epson: matches */
+    /* MX920 + PageSize=TrayJ: the Brainstorm ID tray template is a 10.01in page, Canon's tray-J medium is 8.85in
+       (ESC (p 3071x5311). Acrobat + the Windows driver centre the page on the medium, so the template's card
+       positions assume the page top sits 0.58in above the medium's top edge. Gutenprint anchors the page top at the
+       medium's top and so printed every card 14.5mm too far toward the trailing edge (measured 2026-09-16).
+       Raster row 0 already sits 0.589in below the page top here, so sending no initial ESC (e advance reproduces
+       the centred placement to within 0.01in. */
+    if ( !(strcmp(canon_get_model_capabilities(v)->name,"PIXMA MX920")) && stp_get_string_parameter(v, "PageSize")
+         && !strcmp(stp_get_string_parameter(v, "PageSize"), "TrayJ") ) {
+      privdata->top = 0;
+      /* horizontal: privdata->left is already 0 here; the 1 mm left correction (2026-09-16) lives in the TrayJ
+         paper's left margin (standard.xml 12.84pt): raster column 0 sits at the ESC (p origin, so a larger page
+         margin moves the page content left. */
+    }
     privdata->page_width = page_right - page_left; /* checked in Epson: matches */
     privdata->page_height = page_bottom - page_top; /* checked in Epson: matches */
   }
@@ -5902,13 +5958,15 @@
 		out_channels;	/* Output bytes per pixel */
 #endif
   unsigned	zero_mask;
-  int           print_cd = (media_source && (!strcmp(media_source, "CD")));
+  int           print_cd_slot = (media_source && (!strcmp(media_source, "CD")));
+  int           print_cd = (media_source && (!strcmp(media_source, "CD")) && !(stp_get_string_parameter(v, "PageSize") && !strcmp(stp_get_string_parameter(v, "PageSize"), "TrayJ")));
   int           image_height;
 #if 0
   int           image_width;
 #endif
   double        k_upper, k_lower;
   unsigned char *cd_mask = NULL;
+  int no_cd_mask = 0;
   double outer_r_sq = 0;
   double inner_r_sq = 0;
   unsigned char* weave_cols[4] ; /* TODO clean up weaving code to be more generic */
@@ -6290,6 +6348,11 @@
   stp_allocate_component_data(v, "Driver", NULL, NULL, &privdata);
 
   privdata.emptylines = 0;
+  privdata.pending_empty_blocks = 0;
+  /* Canon's driver replaces blank raster blocks with ESC (e advances; the code path exists above but stays off:
+     blank ESC (F blocks print correctly on the MX922 and the advance variant has not been verified on paper. */
+  privdata.skip_empty_blocks = 0; (void)print_cd_slot;
+  no_cd_mask = stp_get_boolean_parameter(v, "CDNoMask");
   if (print_cd) {
     cd_mask = stp_malloc(1 + (privdata.out_width + 7) / 8);
     outer_r_sq = (double)privdata.cd_outer_radius * (double)privdata.cd_outer_radius;
@@ -6309,7 +6372,7 @@
 	  break;
 	}
     }
-    if (print_cd)
+    if (print_cd && !no_cd_mask)
       {
 	int x_center = privdata.cd_outer_radius * privdata.mode->xdpi / 72;
 	int y_distance_from_center =
@@ -6334,7 +6397,7 @@
 	      }
 	  }
       }
-    stp_dither(v, y, duplicate_line, zero_mask, cd_mask);
+    stp_dither(v, y, duplicate_line, zero_mask, no_cd_mask ? NULL : cd_mask);
     if ( privdata.mode->flags & MODE_FLAG_WEAVE )
         stp_write_weave(v, weave_cols);
     else if ( caps->features & CANON_CAP_I)
@@ -6658,9 +6721,25 @@
     /* a new raster block begins */
     if(!(y % raster_lines_per_block)){
         if(y != 0){
-            /* write finished blocks */
+            /* Canon's own driver never sends blank raster blocks: it skips blank bands with ESC (e advances
+               (captured MX922 job: ESC (e 9, 4 rows, ESC (e 107, then the inked rows). Do the same for the MX920
+               family in disc-tray mode: a block is empty when every channel compressed to one 0x80 terminator per
+               line, i.e. its length equals raster_lines_per_block. */
+            int all_empty = 1;
             for(i=0;i<pd->num_channels;i++)
-                canon_write_block(v,pd,pd->comp_buf + i * max_length,pd->channels[i].comp_buf_offset);
+                if((pd->channels[i].comp_buf_offset - (pd->comp_buf + i * max_length)) != raster_lines_per_block)
+                    all_empty = 0;
+            if(all_empty && pd->skip_empty_blocks){
+                pd->pending_empty_blocks += 1;
+            } else {
+                if(pd->pending_empty_blocks){
+                    canon_cmd(v,ESC28,0x65, 2, (pd->pending_empty_blocks >> 8) & 0xff, pd->pending_empty_blocks & 0xff);
+                    pd->pending_empty_blocks = 0;
+                }
+                /* write finished blocks */
+                for(i=0;i<pd->num_channels;i++)
+                    canon_write_block(v,pd,pd->comp_buf + i * max_length,pd->channels[i].comp_buf_offset);
+            }
         }
         /* reset start offsets */
         for(i=0;i<pd->num_channels;i++)
--- a/src/main/canon-printers.h
+++ b/src/main/canon-printers.h
@@ -3593,7 +3593,7 @@
     INCH(17/2), INCH(14),       /* max paper width and height */ /* from linux driver v3.90 */
     10, 10, 9, 15,    /*border_left, border_right, border_top, border_bottom */
     &canon_PIXMA_MG5400_slotlist, /* iP7200 uses ESC (r 0x68 command for CD tray only */
-    CANON_CAP_STD0|CANON_CAP_DUPLEX|CANON_CAP_r|CANON_CAP_px|CANON_CAP_P|CANON_CAP_I|CANON_CAP_v|CANON_CAP_XML|CANON_CAP_BORDERLESS,0,
+    CANON_CAP_STD0|CANON_CAP_DUPLEX|CANON_CAP_r|CANON_CAP_rr|CANON_CAP_px|CANON_CAP_P|CANON_CAP_I|CANON_CAP_v|CANON_CAP_XML|CANON_CAP_BORDERLESS,0,
     3,9, /* ESC (l and (P command lengths */
     1, /* Upper/Lower Cassette option */
     control_cmd_PIXMA_MG5300,  /* features Uses ESC (r only for CD media */
--- a/src/xml/papers/standard.xml
+++ b/src/xml/papers/standard.xml
@@ -94,6 +94,17 @@
       <height value="329"/>
       <unit value="english"/>
     </paper>
+    <paper name="TrayJ">
+      <description translate="value" value="Disc Tray J - full face"/>
+      <comment value="Canon disc tray J, whole tray face 5.16x10.01in (PVC card trays)"/>
+      <width value="371.52"/>
+      <height value="720.72"/>
+      <left value="12.84"/>
+      <right value="15.4"/>
+      <top value="50.8"/>
+      <bottom value="55.3"/>
+      <unit value="english"/>
+    </paper>
     <paper name="CD3Inch">
       <description translate="value" value="CD - 3 inch"/>
       <comment value="CD, 65mm diameter"/>
